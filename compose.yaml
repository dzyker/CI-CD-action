services:
  app:
    image: dzyker/express_app:latest
    ports:
      - "8080:8080"
    restart: always

  watchtower:
    image: containrrr/watchtower
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    command: --intervar 30
    restart: always
